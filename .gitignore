node_modules/

# Database dumps - contains PII (real names/addresses from QRZ lookups).
# NEVER commit.
backups/
*.sql
!backend/init-db/01-schema.sql
!backend/src/migrations/*.sql
# Demo seed for screenshots — fictional data only, safe to commit.
!scripts/screenshots/demo-seed.sql
# One-off dedup script — SQL only, no dumped data.
!scripts/dedup-tz-shifted-qsos.sql
.env
.env.*
!.env.example
# Documented template for the throwaway screenshot env (no real secrets).
!scripts/screenshots/.env.demo.example
backend/results.txt
client/build/


# Claude Code local settings (shared .claude/ settings stay tracked)
.claude/settings.local.json
